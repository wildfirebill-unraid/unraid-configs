<?xml version="1.0"?>
<Container version="2">
  <Name>qbit-monitor-rss</Name>
  <Repository>ghcr.io/wildfirebill-unraid/qbit-monitor-rss:latest</Repository>
  <Registry>https://ghcr.io</Registry>
  <Network>bridge</Network>
  <MyIP/>
  <Shell>bash</Shell>
  <Privileged>false</Privileged>
  <Support>https://github.com/wildfirebill-unraid/unraid-configs</Support>
  <Project>https://github.com/wildfirebill-unraid/qbit-monitor-rss</Project>
  <Overview><![CDATA[Self-hosted web app that monitors <strong>multiple qBittorrent instances</strong> and automatically adds new releases from <strong>private-tracker RSS feeds</strong> to the instance of your choice, under a configurable <strong>50-200 slot cap</strong> with zero cross-instance duplicates.
<ul>
  <li>Multi-instance qBittorrent support (one tab per instance)</li>
  <li>RSS 2.0 + Atom feed parsing (enclosure and &lt;link href&gt; torrents)</li>
  <li>Dedup by info-hash (added + live client) and normalized title</li>
  <li>Configurable slot cap (50-200) with slot release after a seed-time requirement</li>
  <li>Manual + automatic feed scans</li>
  <li>Persistent state (SQLite + config.json) in /data</li>
  <li>Built-in web GUI, no external frontend build</li>
  <li>Multi-arch images (linux/amd64 + linux/arm64) on GHCR</li>
</ul>
Documentation: <a href="https://github.com/wildfirebill-unraid/qbit-monitor-rss">GitHub repo</a>]]></Overview>
  <Category>Downloaders:Web</Category>
  <WebUI>http://[IP]:[PORT:8200]</WebUI>
  <TemplateURL>https://raw.githubusercontent.com/wildfirebill-unraid/unraid-configs/main/templates/qbit-monitor-rss/qbit-monitor-rss.xml</TemplateURL>
  <Icon>https://raw.githubusercontent.com/wildfirebill-unraid/unraid-configs/main/templates/qbit-monitor-rss/icon.svg</Icon>
  <Description>Monitor multiple qBittorrent instances and auto-add private-tracker RSS items under a configurable 50-200 slot cap.</Description>
  <Networking>
    <Mode>bridge</Mode>
    <Publish>
      <Port>
        <HostPort>8200</HostPort>
        <ContainerPort>8000</ContainerPort>
        <Protocol>tcp</Protocol>
      </Port>
    </Publish>
  </Networking>
  <Data>
    <Volume>
      <HostDir>/mnt/user/appdata/qbit-monitor-rss</HostDir>
      <ContainerDir>/data</ContainerDir>
      <Mode>rw</Mode>
    </Volume>
  </Data>
  <Environment/>
  <Labels/>
  <Config Name="WebUI Port" Target="8000" Default="8200" Mode="tcp" Description="Container port for the web UI (fixed at 8000). Change the Default to map a different host port on Unraid." Type="Port" Display="always" Required="true" Mask="false"/>
  <Config Name="AppData" Target="/data" Default="/mnt/user/appdata/qbit-monitor-rss" Mode="rw" Description="Persistent storage for the SQLite database and config.json. Back this folder up to keep your instances, feeds, and slot state." Type="Path" Display="always" Required="true" Mask="false"/>
</Container>
